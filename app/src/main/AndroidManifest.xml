<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.of.servicebook.michael" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="by.of.servicebook.michael.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="AA_DB_NAME"
            android:value="ServiceBook.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />

        <activity
            android:name="by.of.servicebook.michael.activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="by.of.servicebook.michael.activities.CarDetailsActivity"
            android:label="@string/title_activity_car_details" >
        </activity>
        <activity
            android:name="by.of.servicebook.michael.activities.DetailRecordActivity"
            android:label="@string/title_activity_detail_record" >
        </activity>
        <activity
            android:name="by.of.servicebook.michael.activities.SplashActivity"
            android:label="Michael-admin" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="by.of.servicebook.michael.activities.LoginActivity"
            android:label="@string/title_activity_login" >
        </activity>
    </application>

</manifest>
